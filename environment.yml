name: orfbounder
channels:
  - conda-forge
  - bioconda
dependencies:
  - python=3.12
  - pip
  # Run `conda env create -f environment.yml` from the repository root.
  # pip uses pyproject.toml as the single source of runtime dependencies.
  - pip:
      - .
